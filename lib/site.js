/**
 * THE SITE FILE - who this board belongs to.
 *
 * This is the file the mentor writes the moment they say their name, so the
 * greeting is theirs. It lives in the repo on purpose: the name travels with
 * the board to every device, instead of living in one browser's storage.
 *
 * A name saved in the app's own store (a later episode) wins over this file;
 * this is the seed, and it is enough.
 */
window.SITE = {
  name: '',
}
